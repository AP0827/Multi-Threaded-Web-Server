# MTWS comparison stack tuning file.
# Keep this file intentionally small at first so the proxy path stays as close
# as possible to a stock ModSecurity + CRS deployment.
#
# Add request-phase exclusions here only when you need to demonstrate how much
# manual tuning a split-proxy WAF requires compared to MTWS.
